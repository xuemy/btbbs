<?xml version="1.0" encoding="UTF-8"?>
<urlset>
    {% for d in data %}
        <url>
            <loc>{{ request.get_host }}{{ d.get_absolute_url }}</loc>
            <lastmod>2013-01-01</lastmod>
            <changefreq>always</changefreq>
            <priority>1.0</priority>
            <data>
                <display>
                    <name>{{ d.name }}</name>
                    {% for aka in d.aka %}
                        <alias>
                            <![CDATA[{{ aka }}]]>
                        </alias>{% endfor %}
                    <image>{{ d.get_image }}</image>
                    <description><![CDATA[{{ d.summary }}]]></description>
                    {% for genre in d.genres %}
                        <genre><![CDATA[{{ genre }}]]></genre>
                    {% endfor %}
                    {% for cast in d.casts %}
                    <actor>
                            <name><![CDATA[{{ cast }}]]></name>
                    </actor>
                    {% endfor %}
                    {% for director in d.directors %}
                        <director>
                            <name><![CDATA[{{ director }}]]></name>
                        </director>
                    {% endfor %}
                    {% for writer in d.writer %}
                        <author>
                            <name><![CDATA[{{ writer }}]]></name>
                        </author>
                    {% endfor %}
                    <inLanguage>{{ d.language|first }}</inLanguage>
                    <contentLocation>{{ d.countries|first }}</contentLocation>
                    <premiere>
                        <datePublished>{{ d.pubdate|date:"Y-m-d" }}</datePublished>
                    </premiere>
                    <aggregateRating>
                        <ratingValue>{{ d.rating }}</ratingValue>
                        <bestRating>10</bestRating>
                    </aggregateRating>
                </display>
            </data>
        </url>
    {% endfor %}
</urlset>
